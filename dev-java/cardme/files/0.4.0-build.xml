<project name="cardme" default="jar" basedir=".">
	<property name="src" location="src" />
	<property name="tests" location="tests" />
	<property name="build" location="build" />
	<property name="build.test" location="build-test" />
	<property name="dist" location="dist" />

	<target name="init">
		<mkdir dir="${build}" />
	</target>

	<target name="compile" depends="init" description="compile the source ">
		<javac srcdir="${src}" destdir="${build}" />
	</target>

	<target name="jar" depends="compile" description="generate the distribution">
		<jar jarfile="${dist}/${ant.project.name}.jar" basedir="${build}" />
	</target>

	<target name="javadoc" description="Generates the Javadoc of the application">
		<javadoc sourcepath="${src}" packagenames="*" destdir="apidocs"
			access="protected" old="false" verbose="false" encoding="UTF-8"
			version="true" use="true" author="true" splitindex="false"
			nodeprecated="false" nodeprecatedlist="false" notree="false" noindex="false"
			nohelp="false" nonavbar="false" serialwarn="false" charset="ISO-8859-1"
			docencoding="UTF-8" source="1.5" linksource="true" breakiterator="false">
		</javadoc>
	</target>

	<target name="compile-tests">
        <mkdir dir="${build.test}" />
        <javac destdir="${build.test}" nowarn="false" debug="true" optimize="true" deprecation="true" target="1.5" verbose="false" fork="false" source="1.5">
            <src>
                <pathelement location="${tests}" />
            </src>
            <classpath>
                <pathelement location="${build}" />
            </classpath>
        </javac>
    </target>

    <target name="test" depends="compile-tests">
        <junit printsummary="yes" haltonfailure="false">
            <classpath>
                <pathelement location="${build}" />
                <pathelement location="${build.test}" />
            </classpath>

            <formatter type="plain" />

            <batchtest fork="yes">
                <formatter type="plain" />
                <fileset dir="${tests}">
                    <include name="**/Test*.java" />
                    <include name="**/*Test.java" />
                    <exclude name="**/*$*.class" />
                </fileset>
            </batchtest>
        </junit>
    </target>
</project>