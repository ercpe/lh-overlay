--- instlib/source/Makefile.proto	2009-07-03 12:16:36.000000000 +0200
+++ instlib/source/Makefile.proto.new	2009-07-03 14:37:43.000000000 +0200
@@ -25,7 +25,7 @@
 
 # rule for the fortran routines
 $(OBJS): 
-	@ echo "compiling: $(@:.o=.f)"; \
-	  $(F77) -c $(F77FLAGS) $(@:.o=.f)
+	  $(F77) -c $(F77FLAGS) $(@:.o=.f)
+ 
 
 # rule for the dynamic memory allocation C routines
@@ -41,7 +41,7 @@
 # rule for the executable itself
 ../bin/cns_solve: $(DEPENDS) 
 	@ echo ; echo "linking: cns_solve"; echo
-	@ cns_string=`date "+%y%m%d%H%M"`; \
+	cns_string=`date "+%y%m%d%H%M"`; \
 	  $(LD) $(LDFLAGS) -o cns_solve-$$cns_string.exe $(OBJS) dmemory.o machine_c.o \
 	                                      $(CNS_FFTDIR) $(CNS_FFTLIB) $(OMP_LIB); \
 	  if [ -x cns_solve-$$cns_string.exe ]; \
--- instlib/source/Makefile.proto	2009-07-03 15:16:10.000000000 +0200
+++ instlib/source/Makefile.proto.new	2009-07-03 15:21:33.000000000 +0200
@@ -31,12 +31,12 @@
 # rule for the dynamic memory allocation C routines
 dmemory.o: dmemory.c
 	@ echo ; echo "compiling: $?"
-	@ $(CC) -c $(CCFLAGS) $?
+	$(CC) -c $(CCFLAGS) $?
 
 # rule for the machine specific C routines
 machine_c.o: machine_c.c
 	@ echo ; echo "compiling: $?"
-	@ $(CC) -c $(CCFLAGS) $?
+	$(CC) -c $(CCFLAGS) $?
 
 # rule for the executable itself
 ../bin/cns_solve: $(DEPENDS) 
@@ -58,7 +58,7 @@
 	@ echo; echo "flags:"; \
 	  echo " fortran -> [$(F77)] $(F77FLAGS)"; \
 	  echo "       c -> [$(CC)] $(CCFLAGS)"; \
-	  echo "    link -> [$(LD)] $(LDFLAGS) $(CNS_FFTDIR) $(CNS_FFTLIB)"; \
+	  echo "    link -> [$(LD)] $(LDFLAGS) $(CNS_FFTDIR) $(CNS_FFTLIB) $(OMP_LIB)"; \
 	  echo
 
 # regenerate makefiles
